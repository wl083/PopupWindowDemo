<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 效果1  -->
    <!--<style name="popAnimation">-->
        <!--&lt;!&ndash;<item name="android:windowEnterAnimation">@anim/popwindow_enter</item>&ndash;&gt;-->
        <!--<item name="android:windowExitAnimation">@anim/popwindow_exit</item>-->
    <!--</style>-->

    <!-- 效果2  -->
 <style name="popAnimation">
        <item name="android:windowEnterAnimation">@anim/enter</item>
        <item name="android:windowExitAnimation">@anim/popwindow_exit</item>
    </style>

    <style name="popAnimOne">
        <item name="android:windowEnterAnimation">@anim/anim_pop_gradual_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_pop_gradual_exit</item>
    </style>


    <style name="AnimScale">
        <item name="android:windowEnterAnimation">@anim/scal_in</item>
        <item name="android:windowExitAnimation">@anim/scal_out</item>
        <item name="android:activityOpenEnterAnimation">@anim/scal_in</item>
        <item name="android:activityOpenExitAnimation">@anim/scal_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/scal_in</item>
        <item name="android:activityCloseExitAnimation">@anim/scal_out</item>
    </style>
    <style name="FrameDialogTheme" parent="android:Theme.Holo.Light.Dialog">
        <item name="android:windowBackground"> @android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
